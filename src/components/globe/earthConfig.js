/*
 * Earth, Atmosphere and EarthClouds are separate sibling meshes (not
 * nested), so they each need the same base radius to actually line up -
 * this is the one place that number lives.
 */
export const EARTH_RADIUS = 1.25;
